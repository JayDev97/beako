<p id="notice"><%= notice %></p>

<h1>Budget Cost Items</h1>

<table>
  <thead>
    <tr>
      <th>Item</th>
      <th>Quantity</th>
      <th>Unit</th>
      <th>Price</th>
      <th>Total</th>
      <th>Budget</th>
      <th>Account</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @budget_cost_items.each do |budget_cost_item| %>
      <tr>
        <td><%= budget_cost_item.item %></td>
        <td><%= budget_cost_item.quantity %></td>
        <td><%= budget_cost_item.unit_id %></td>
        <td><%= budget_cost_item.price %></td>
        <td><%= budget_cost_item.total %></td>
        <td><%= budget_cost_item.budget_id %></td>
        <td><%= budget_cost_item.account_id %></td>
        <td><%= link_to 'Show', budget_cost_item %></td>
        <td><%= link_to 'Edit', edit_budget_cost_item_path(budget_cost_item) %></td>
        <td><%= link_to 'Destroy', budget_cost_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Budget Cost Item', new_budget_cost_item_path %>
